# Documentation Audit — 2026-06-24

Audit of all docs against current implementation. Terraform was fully removed (replaced by
Packer + CDK), script numbering shifted, and several directories/scripts were renamed or
deleted. Many docs still describe the old state.

Status legend: `[ ]` open · `[x]` done

---

## 🔴 Critical — Completely Wrong, Needs Rewrite

### [ ] `DEPLOYMENT_GUIDE.md` (root)
- Entire "Infrastructure Setup" step instructs `terraform init/plan/apply` with
  `terraform.tfvars` inside an `infrastructure/` directory that does not exist
- Terraform outputs referenced (`control_plane_public_ip`, `worker_node_instance_profile`,
  `terraform output ssh_command`) do not exist
- Step 3 tells users to run `./install.sh` — actual entry point is `setup-eks-d.sh`
- Step 4 references `karpenter-config/install-karpenter.sh` — directory does not exist;
  Karpenter is installed by `eks-d-setup/15-install-karpenter.sh`
- Cleanup section uses `terraform destroy`
- Security section lists Terraform as a dependency
- **Fix:** Rewrite around the deployment bundle (`docker run ghcr.io/.../eks-d-xpress-bundle`)
  and point to `setup-eks-d.sh` for the control-plane boot sequence

### [ ] `docs/user-guides/deployment.md`
- Step 1: `./provision-shared-infra.sh` — script does not exist
- Step 2: `./deploy-developer.sh` — script does not exist
- Step 4: `cd ~/ecp-eks-dx-infra/karpenter-config` — wrong repo name and path
- Manual setup step clones `ecp-eks-dx-infra` and runs `./install-all.sh` — neither exists
- Cleanup references CloudFormation stacks `eks-d-alice` and `eks-d-shared-vpc` — the
  stack names may still be valid but the provisioning side is wrong
- **Fix:** Rewrite to match current flow

---

## 🟠 Significant — Wrong Script Paths / Numbers

### [ ] `docs/user-guides/components.md`

| Stated in doc | Actual |
|---|---|
| `eks-d-setup/06-install-eks-d.sh` | `eks-d-setup/07-install-eks-d.sh` |
| `eks-d-setup/07-install-cni.sh` | `eks-d-setup/08-install-cni.sh` |
| `eks-d-setup/08-install-coredns.sh` | Does not exist — CoreDNS comes with kubeadm |
| `eks-d-setup/09-install-ebs-csi.sh` | `eks-d-setup/13-install-ebs-csi.sh` |
| `karpenter-config/install-karpenter.sh` | `eks-d-setup/15-install-karpenter.sh` |
| `eks-d-setup/11-install-karpenter.sh` (also listed) | `eks-d-setup/15-install-karpenter.sh` |
| `eks-d-setup/02-install-docker.sh` (supporting table) | Not in `eks-d-setup/`; Docker lives in AMI builder |
| `eks-d-setup/03-install-kubectl.sh` | Does not exist as a separate script |
| `eks-d-setup/04-install-helm.sh` | `ami-builder/scripts/04-install-helm.sh` |
| File location: `karpenter-config/` | Does not exist |
| File location: `infrastructure/` (CloudFormation templates) | Does not exist |

### [ ] `AGENTS.md`
- `setup-eks-d.sh` listed as **5,487 LOC** — actual: **135 lines**
- `ami-builder/scripts/install.sh` listed as 515 LOC — actual: 517 (negligible)

---

## 🟡 Outdated Context / Stale References

### [ ] `docs/user-guides/architecture.md`
- Data flow sequence diagram has a `CloudFormation` participant deploying developer stacks —
  current architecture uses a Lambda-driven provisioner, not manually triggered per-developer
  CF stacks
- **Fix:** Update sequence diagram to reflect bundle + Lambda provisioner flow

### [ ] `docs/user-guides/index.md`
- Broken link: `../../infrastructure/SECURITY.md` — `infrastructure/` directory does not exist
- **Fix:** Remove or replace the link

### [ ] `docs/MIGRATION_TO_AWS_NATIVE.md`
- Written as a forward-looking migration plan ("Terraform → AWS-Native") but **Terraform is
  already gone** — Packer migration completed, CDK is live
- SSM path prefix uses `/eks-dx/` throughout, but `AMI_PIPELINE_SETUP.md` and the actual CDK
  use `/eks-d-xpress/`
- **Fix:** Add completed/historical header; correct SSM prefix; or move to
  `docs/design/historical/`

### [ ] `docs/POD_IDENTITY_INTEGRATION.md`
- References "Step 14 / `14-install-eks-dx-pod-identity.sh`" — numbered script is
  `12-install-eks-dx-pod-identity.sh`
- States "Not wired into `setup-eks-d.sh` boot sequence" — needs verification against current
  `setup-eks-d.sh` (may already be wired)
- **Fix:** Correct script number; verify and update wiring status

---

## 🟢 Archive / Label as Historical

### [ ] `docs/design/packer_migration/README.md`
- Migration proposal for replacing Terraform-based AMI building with Packer — **this is done**
- **Fix:** Add `> ⚠️ Historical — completed` header

### [ ] `docs/design/aws-ec2-setup/06-infrastructure.md`
- All fixes framed as Terraform HCL changes (`terraform/main.tf`)
- IMDSv2 hop limit and security group rules are now handled via Launch Templates in CDK
  (`EksDxSharedInfraStack`)
- **Fix:** Add historical note; annotate that fixes were applied via CDK

### [ ] `docs/design/aws-ec2-setup/10-karpenter-requirements.md`
- "Created by Terraform (`aws_sqs_queue.karpenter_interruption` in `terraform/main.tf`)"
- **Fix:** Update to reflect that SQS queue is now provisioned by the CDK infra stack

### [ ] `docs/design/aws-ec2-setup/02-aws-vpc-cni.md`
- "IAM: `AmazonEKS_CNI_Policy` on instance role | ✓ Attached in Terraform"
- **Fix:** Replace "in Terraform" with current mechanism (CDK infra stack / Launch Template IAM)

### [ ] `docs/design/aws-ec2-setup/03-coredns.md`
- References `install-all.sh`
- **Fix:** Replace with `setup-eks-d.sh` or mark as historical

### [ ] `docs/design/aws-ec2-setup/04-cloud-provider.md`
- References `install-all.sh`
- **Fix:** Replace with `setup-eks-d.sh`

### [ ] `docs/design/aws-ec2-setup/07-install-order.md`
- References `install-all.sh`
- **Fix:** Replace with `setup-eks-d.sh`

### [ ] `docs/design/shared-infra-cdk-migration.md`
- SSM path prefix `/eks-dx/` vs actual `/eks-d-xpress/` used in `AMI_PIPELINE_SETUP.md`
- Tenant Terraform data lookups described (e.g. `data "aws_vpc"`) — Terraform is gone
- **Fix:** Correct SSM prefix; annotate that migration is in progress in `eks-d-xpress-infra`
  repo, not in this one

### [ ] `docs/design/workstation-api.md`
- References `terraform/api/` directory (does not exist)
- References per-developer Terraform state management
- **Fix:** Add historical/design-only note or update to reflect Lambda provisioner

### [ ] `docs/design/proposal/minimax-2.5-iac-improvements.md` and `...-review.md`
- Terraform improvement proposals that are moot now that Terraform is removed
- **Fix:** Add historical header

### [ ] `.agents/summary/data_models.md`
- "Terraform structures" listed as a content category
- **Fix:** Remove or rename to "CDK / CloudFormation structures"

### [ ] `.agents/summary/review_notes.md`
- "Terraform variable customization" listed as a configuration recommendation
- **Fix:** Remove; replace with CDK context key customization if applicable

### [ ] `.agents/summary/index.md`
- Describes `data_models.md` as covering "Terraform structures, Kubernetes resources..."
- **Fix:** Update description
